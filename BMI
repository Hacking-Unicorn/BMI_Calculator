
function bmiCalculator(weight, height) {
    var bmi = weight / (height**2);
    return Math.round(bmi);
}


var bmi = bmiCalculator(prompt("What is your weight?"), prompt("What is your height?"));

console.log(bmi);
